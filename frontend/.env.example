# URL base del backend Spring Boot.
# Se usa solo cuando VITE_USE_MOCK=false.
VITE_API_BASE_URL=http://localhost:8080/api

# Switch entre datos mock (sources2.0) y backend real.
# Mantener en true durante el desarrollo del frontend.
VITE_USE_MOCK=true
